#include <iostream>
#include <vector>

using std::cout;
using std::endl;

int FindSubArray(int,int,int);
std::vector <int> CreateVector(int);
std::vector <int> AddValuesToVector(int[]);
std::vector <int> PrintSubArrayIndex(std::vector <int>,int, int);

int main() {

  FindSubArray(2,5,12)
  return 0;
}
int FindSubArray(int num_cases,int vector_size, int sub_array_sum){
 for(int x ; x = num_cases ; x++){
   CreateVector(vector_size);

 }
}
std::vector<int> CreateVector(int vector_size){
  std::vector<int> users_input;
  users_input.resize(vector_size);
}

std::vector <int> PrintSubArrayIndex(std::vector <int> sub_array, int start_at, int end_at){

}